# tuples
    # Armazena mais de uma informacao em variaveis
    # Mantem a sequencia dos dados em uma variavel
    # Nao podem ser alteradas


cores_list = ['amarelo', 'verde', 'azul', 'vermelho']
cores_tuple = ('amarelo', 'verde', 'azul', 'vermelho')


print(cores_list)
print(cores_tuple)

print(type(cores_list))
print(type(cores_tuple))
